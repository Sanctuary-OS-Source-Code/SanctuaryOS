**Date: September 2, 2026**
**Version: 0.5.7**
## TITLE

### Visual Overhauls
- **Mobile Elevated Hub Redesign:** Replaced the massive blocky grid navigation tabs on mobile viewports for all Elevated Hubs with a sleek, horizontally scrolling `StatTileCarousel`. This maintains the premium desktop "pill" styling without sacrificing excessive vertical screen real estate.
- **MasonRegistry Artifacts View:** Shrunk the 'Recent Artifacts' and 'Issues' feed cards down from their massive vertical layouts to sleek `horizontal` variants, allowing many more artifacts to be visible simultaneously without overwhelming the screen.
- **Mobile Elevated Roles:** Dynamically injected the `MasonHub` shortcut directly into the primary mobile bottom navigation bar (instead of hiding it in the 'More' drawer) for any user with elevated roles, massively improving mobile moderation workflows.
- **Lexicon Patch:** Restored missing dictionary keys (`title_issues`, `no_issues`) across the environment lexicons.
- **Global Tag System Enforcement**: Standardized the entire OS lexicons (`en-default`, `en-sanctuary`, `en-sims`, `Simlish`) to strictly use six approved mod status tags: `Stable`, `Unstable`, `Corrupted`, `Under Review`, `Pending`, and `Unknown`. Refactored `SharedRegistry.tsx` (MasonHub) to use the `ElevatedHubLayout` tile-style tabs for displaying status-based filtering (Overview, Stable, Under Review, Pending, Unknown, Unstable & Corrupted). Modernized the Overview tab into a structured 2-column dashboard (Recent Activity | Issues), and integrated an `ActionPill` component for filtering by category globally, replacing legacy inline headers.
- **Conflict Radar Refinement:** Overhauled the `DbpfScout.tsx` layout and selection flow to scale effectively with hundreds of blueprints. The landing page now correctly utilizes a compact `UniversalCard` auto-fill grid alongside correctly wrapped `CommandScreenStats`. On the Conflicts tab, massive blocky filter buttons have been completely replaced with a sleek, horizontal `GlassSegmentedControl` component (aligning perfectly with the Mason IDE aesthetic) to save vertical space while retaining inline numerical badging.
- **HoverTooltip Standardization:** Eliminated native browser `title` tooltips across the entire OS. Migrated `<button>` elements in the `ArchitectConflictMatrix`, `MasonHeader`, and `CitizenTickets` as well as generic components like `ActionPill` and `UniversalEditors` to correctly utilize the custom `<HoverTooltip>` component. This comprehensively resolves visual inconsistencies where default white browser tooltips (such as "Create Conflict Rule" or "Toggle Layout") would appear over interactive elements.
- **Side Panel Filter Box Styling:** Updated the `SearchBar` and `FilterPopover` components within `ScreenUtilityBar` to accept a `variant="panel"` prop. This prop applies a subtle `glass-surface` override for these elements when nested inside Side Panels, ensuring they maintain proper visual hierarchy and contrast against the panel's distinct background.
- **Global Theme Localization:** Updated the core Chameleon Theme Engine to dynamically label global themes outside of workspaces (like on the web) as "Community Defaults" using proper Lexicon routing (`badge_community_defaults`), replacing the hardcoded "Sanctuary" fallback badge and enabling translated labels like "Global Blueprints" and "Townie Styles".
- **Web Chameleon Vault Syncing:** Overhauled the `fetchGameThemes` lifecycle in `ThemeContext.tsx` to automatically query the Core `sanctuary_games` database for active public Spoke credentials. This ensures that Partner/Community themes (like The Sims 4) are globally fetched and appropriately badged under their specific game names (e.g., "The Sims 4") in the web application, completely circumventing the requirement for an active local Workspace.
- **Top Navigation Row Standardization:** Replaced the desktop Sidebar Navigation (`HoverTabDrawer`) with a horizontal `StatTileCarousel` row at the top of all 7 major sections (`Vault.tsx`, `Blueprints.tsx`, `Nexus.tsx`, `GlobalFeed.tsx`, `DbpfScout.tsx`, `CitizensWorkbench.tsx`, and `TimeCapsule.tsx`). The original Sidebar continues to function as the primary mobile navigation layer (`md:hidden`). Additionally, tightened the vertical spacing between tab tiles and filter tiles in `Lab.tsx`, while migrating redundant dashboard `StatTiles` into sleek Quick Links on overview screens to streamline interactions.
- **Mobile Navigation Stability:** Fixed a critical rendering crash within `HoverTabDrawer` by properly initializing its `mobileOpen` state. This restores the core horizontal navigation pills across the Comm-Link Feed (`GlobalFeed.tsx`) and Nexus feeds on mobile viewports.
### Security & Architecture
- **Visibility Rules & Content Safeguards**: Overhauled `Nexus.tsx` queries to strictly surface mods with `compliance_tier = 0` and appropriate public statuses (`stable`, `unstable`, `corrupted`, `pending`). Configured Blueprint visibility and `Lab.tsx` cloud interactions to similarly limit fetches to `compliance_tier = 0` assets and approved tags, completely hiding queue items from unauthorized feeds.
- **Dynamic Schema-Driven PE Parsing:** Overhauled `rip_game_version` in the Tauri backend to automatically extract standard PE file version metadata (like `FileVersion`) natively from game executables (`.exe`) using the `pelite` Rust crate. This completely avoids PowerShell subprocesses, removing a common AV heuristic trigger and improving radar sweep performance. The parser intelligently utilizes `executable_names` from the active master schema (e.g. `sims4.json`) to locate predefined binaries first (like `TS4_x64.exe`). It falls back to scanning the target directory for the largest `.exe`—aggressively filtering out known crash handlers, redistributables, and anti-cheat binaries—before finally relying on the schema-defined `.ini` manifest (`version_extraction.relative_path`) if native PE extraction fails.
- **Editor Asset Extraction:** Extracted the asset fetching, searching, and `SidePanel` UI logic into a new shared abstraction, `LinkAssetSidePanel`. Successfully migrated all 6 editor components (`WayfinderPostsEditor`, `KeepersWebNewsEditor`, `KeepersWebDirectivesEditor`, `KeepersDispatchEditor`, `KeeperPostsEditor`, `MasonPostsEditor`) to use this standardized shared component, ensuring consistent tier-0 mod filtering globally.
- **Global Documentation & Schema Sync:** Synchronized 7 core architecture documents and SQL schemas (`schema_os.sql`, `schema.sql`, `Citizen Guide.md`, `Master Architecture & Feature Document.md`, `Sanctuary OS Forking & Architecture Guide.md`, `Compliance Protocol.md`, and `Partners Agreement.md`) to reflect recent systemic changes. Added the `hardware_bans` table to the OS schema and the `mod_meta` JSONB column to Blueprints. Deprecated all legacy references to Rust DBPF parsers in favor of the dynamic DB-driven expansion models, and updated the Nuclear Override documentation to reflect the new decoupled HWID privacy logic.
- **Hardware Ban Decoupling:** Reworked the Hardware ID (HWID) banning logic to enhance privacy and security. HWIDs are no longer persistently tracked or updated on user profiles upon login. Instead, a new dedicated `hardware_bans` table in the Core OS database stores HWID hashes only when a ban is explicitly triggered (e.g., via Nuclear Override or Malware Evasion). Login checks now cross-reference this table and enforce bans across both the Core OS and Spoke (Game) databases dynamically without requiring continuous tracking of the user's hardware signature.
- **OS-Wide Mobile Filter Architecture:** Refactored the core `ScreenUtilityBar` engine to use a new `MobileFilterGroup` bottom-sheet on mobile devices. This automatically collapses all secondary filters, dropdowns, and actions into a sleek bottom sheet on over 33 screens OS-wide, massively improving mobile header layouts and reclaiming vertical real estate.
- **Comm-Link Mobile Layout Fix:** Updated the mobile filter layout in the Comm-Link Feed (`GlobalFeed.tsx`) to directly align with the sleeker styling of the Nexus Tab. Replaced the generic `ScreenUtilityBar` wrapper with native nested `SearchBar` and `FilterPopover` components inside the `ViewHeader` to ensure the filters properly consolidate into a sleek dropdown sheet rather than crowding the center of the mobile viewport.
- **Comm-Link Feed Overview Rework:** Redesigned the Stat Tiles on the Comm-Link Feed overview screen to be much more engaging and actionable. The tiles now track "Recent Following," "Recent Global," "Interactions," and "Your Network," directly linking out to their respective tabs or a newly built `MasonFollowingSidePanel` to let users quickly manage and browse the masons they follow. Additionally, the core global feed searchbar now intelligently filters by mason profiles (display name and username) alongside standard text content.
- **Offline Metadata Persistence:** Upgraded the Blueprint (PlaySet) import and export engines to natively support full "Local Dossier" metadata overriding. Custom metadata (like `author` and `url`) assigned to "ghost" strings (missing mods) via the Local Dossier (`sanctuary_local_overrides`) is now dynamically injected into the Blueprint during export. Crucially, when a user imports a Blueprint with this custom metadata, it is securely persisted inside the PlaySet schema as `modMeta`, ensuring custom creator URLs remain fully intact even when shared peer-to-peer. The Ghost Strings and Missing Imports UI alerts now intelligently fall back to the global cloud Registry database for URLs and authors if offline metadata is absent.
- **Blueprint Game Identity:** Blueprint (PlaySet) exports now dynamically embed the `game_id` and `game_name` of the active workspace they were exported from, allowing the OS to accurately identify the target environment when the `.json` file is later imported.
- **Local DBPF Parser Deprecation:** Completely removed the local DBPF Rust parser from the codebase (`dbpf.rs` and `parser.rs`) and migrated the Conflict Radar and Explicitly Local logic to fully rely on the dynamic, DB-driven expansion model. This eradicates flakey parsing errors and unifies the schema architecture for easier game integration.
- **Hub Components Migration:** Completed a massive architectural migration of all legacy Hub components (Oversight, Communications, Chameleons, Settings, Wayfinders, and Masons) to the new `ElevatedHubLayout` and `UniversalCard` standards. This fully eradicates custom layout hacks, unifies the visual hierarchy, centralizes utility/filter bars, and guarantees consistent responsive behavior across the entire OS.
- **Spoke Database UUID Linkage:** Overhauled the data architecture for Game Spoke databases (e.g. `nexus_assets`, `nexus_reports`, `blueprint_reports`) to strictly bind records to active user UUIDs (`author_id`, `reporter_id`) rather than relying exclusively on string usernames. This permanently protects content ownership and telemetry mapping in the event of user profile name changes.
- **Finalized Hub Layout Migration:** Completed the final manual migration phase for `SADefcon`, `KeepersWebLegalEditor`, `KeepersWebNewsEditor`, `KeepersWebDirectivesEditor`, and `SAComplianceOversight`. Ensured all hub components implement the standard `renderLanding` overview tab, numerical badge counts powered by frontend dataset `.filter()` logic, and strict Lexicon key mapping.


### Bug Fixes
- **Elevated Hub Persistence & Role Decoupling:** Fixed an architectural logic flaw where swapping workspaces using the SidePanel or Mobile Navigation did not properly re-evaluate the user's role because the `fetchUserRole` function was locked behind a `didInitBoot` ref. Additionally, decoupled the monolithic `userRole` state into two separate roles: `osRole` (Global OS Permissions) and `userRole` (Game Workspace Permissions). Previously, Global Developers were forcefully elevated to "admin" across all workspaces, causing unauthorized tier tabs (MasonHub, ArchitectHub) to remain visible even when testing as a "citizen". Keepers Core and SADefcon now strictly check `osRole`, while game-specific hubs check `userRole`. Furthermore, added automated fallback logic to instantly kick users back to the `dashboard` view if their newly fetched role drops below the required tier for their currently active Elevated Hub. Also fixed a critical race condition where extracting `fetchUserRole` caused it to run before the application finished fetching the `workspaces` array during boot, resulting in a null database client and forcefully downgrading valid roles (like Wayfinder) to "citizen". Fixed by adding `workspacesLength` as a reactive dependency.
- **TimeCapsule Crash:** Fixed a severe layout breakdown in the TimeCapsule grid where missing dictionary keys (`recent_world_states` and `recent_engine_cores`) in the `en-sims` and `en-sanctuary` lexicons caused the header sections to render as empty, throwing the CSS flexbox and CSS grid alignments entirely out of proportion. Synchronized all missing keys dynamically across the environment lexicons.
- **Spoke Display:** Fixed an issue where the active Spoke (Game Database) was not displaying correctly as the badge or community tag for Custom Themes and Custom Lexicons in the settings, integrating them correctly into the filtered lists.
- **Malware Edits Tracking:** Added a dedicated "MALWARE EDITS" filter to the Audit Logs that leverages a custom Supabase `.ilike()` match against the `mods` target table to seamlessly track threat management actions.
- **Mobile Viewport Height:** Fixed an issue where the mobile bottom navigation bar was pushed off-screen by the browser URL bar by replacing legacy `h-screen` classes with modern `h-[100dvh]` viewports across the root App and Auth wrappers.
- **Public Post Read-Only Mode:** Refined read-only mode in `MasonPostViewer` to completely hide the replies container (including the "No Replies Yet" message) on the landing page public broadcasts, along with hiding interaction buttons (Flag, Profile, Reply Form).
- **Sticky Tooltips Resolved:** Fixed a frustration on mobile where `HoverTooltip` overlays would appear on tap and refuse to dismiss by completely disabling hover tooltips on viewports under 768px.
- **Landing Page Navigation Fixes:** Restored the ability to access the Settings menu directly from the Web Landing Page by adding a persistent button group to the top right of the viewport. Re-enabled the System Dock (bottom bar) on the Desktop Landing Page so that users can access their Settings and Terminal logs even when no active workspace is selected. Added a dedicated 'Settings' button to the Sidebar navigation link list on the Landing Page.
- **SidePanel Refactor:** Migrated all legacy `SidePanelActionFooter` implementations across the OS to the new `headerActions` standard (`PanelHeaderGroup` & `PanelHeaderButton`). Removed all explicit 'Cancel' and 'Abort' footer buttons to streamline the UI, relying exclusively on the built-in top-right close (X) button for cancellations. Cleaned up and fully deprecated the `SidePanelActionFooter` component from the codebase.
- **Landing Page Navigation Returns:** Fixed an issue where accessing Settings or Keepers Core from the root domain would trap the user in those views without a way to navigate back to the landing page. All landing-page specific Sidebar links, the Desktop Logo, and the Mobile Bottom Navigation tabs have been updated to correctly restore the landing page view before attempting to scroll to their targets.
- **Landing Page Layout:** Removed the side-by-side split layout between Workspaces and News on the Landing Page. Sections now stack vertically, allowing Workspaces to utilize the full width of the screen in a clean, responsive grid of horizontal banners.
- **Authentication Guidelines:** Added Privacy and Anonymity Guidelines dynamically to the Authentication UI (Sign Up view) using a sleek, interactive feature list with glassmorphic iconography.
- **DEFCON Modal Clipping:** Fixed an issue where the DEFCON Global Override confirmation modal was being incorrectly clipped by `overflow-hidden` constraints and constrained to the bounding box of its parent layout due to CSS transform rules (`animate-in`). Bypassed the layout stacking context by implementing a React `createPortal` to render the critical alert modal directly to the `document.body`, ensuring it correctly covers the entire viewport on all devices.
- **Mobile Glass Legibility:** Resolved catastrophic GPU tearing and scrolling lag (10fps) on mobile viewports caused by `background-attachment: fixed` on glass elements. The "magic bullet" fix was to keep the high-fidelity desktop "fake blur" (the `::before` pseudo-element with the blurred background image), but forcefully override its attachment to `background-attachment: scroll !important;` on mobile. Due to the heavy 24px blur, the fact that the inner background scrolls slightly out of sync with the main wallpaper is imperceptible, resulting in visually identical, buttery smooth 60fps frosted glass across all mobile devices.

### Visual Overhauls
- **PillTabs Segmented Control:** Completely overhauled the `PillTabs` component to act as a sleek, premium segmented control. Replaced instant color toggling with a dynamic, hardware-accelerated sliding glass background highlight that smoothly animates between active tabs, vastly improving micro-animations and component feel across System Status and configuration panels.
- **System Telemetry Rework:** Completely removed the "Working Set vs Private Set" PillTab swap toggle in the System Status Panel. Memory metrics are now displayed cleanly side-by-side without requiring interaction, permanently fixing the confusing and disruptive number-rolling animations caused by the legacy UI toggle.
- **Card Actions & Segmented Navigation Cleanup:** Removed confusing floating icon filters in the Citizens Workbench and replaced them with labeled `PillTabs`. Refactored the bulky action buttons inside Time Capsule cards into a sleek 3-column action tray matching the Vault UI. Eliminated a redundant blueprint selection dropdown in Conflict Radar that cluttered the search bar area.
- **Universal Toggle Standardization:** Eradicated the scattered, custom HTML checkbox implementations (`ugly toggles`) across `SASupportSettings` and `KeeperSupportSettings`. Replaced them entirely with the sleek, unified `UniversalToggle` component nested inside 2-column responsive `glass-surface` grids, ensuring consistent hardware-accelerated micro-animations for all configuration switches.
- **Database Node Editor Header Consolidation:** Migrated the legacy action footer inside the "EDIT DATABASE NODE" panel (`KeepersActiveGames.tsx`) to the unified `headerActions` standard utilizing `PanelHeaderGroup`, fully resolving the last remaining scattered panel action buttons.
- **Global Glass Architecture Rebuild:** Solved pervasive Chromium "tearing" and miniaturization bugs with `background-attachment: fixed` on nested glass panels. Refactored the core `.glass-panel` and `.glass-surface` CSS rules to use `background-image` exclusively instead of the `background` shorthand. This prevents the browser from stripping explicit Tailwind `bg-[]` variables and resolves complex stacking context traps.
- **Authentication UI Refresh:** Rebuilt the `AuthWrapper` from the ground up to utilize a native Tailwind `backdrop-blur-[60px]` layer instead of the pseudo-element hacks. Implemented a responsive layout that expands to `max-w-5xl` to display Network Access Guidelines during registration, while remaining a sleek `max-w-xl` for login. Restored logo legibility by removing heavy opacity dampening and adding a custom drop-shadow. Removed `glass-surface` tearing from the Bypass button.
- **StatTiles Color Injection:** Fixed a bug where Dashboard StatTiles failed to inherit their respective theme colors (appearing as empty transparent outlines). Solved CSS z-index and arbitrary compilation limitations by injecting a dedicated absolute overlay div with an inline `backgroundColor: color-mix()` variable, guaranteeing it renders correctly below text but above the background blur.
- **Mobile Landing Page Optimization:** Hid the native desktop client download cards on the Web Landing Page for mobile viewports, replacing them with a custom Lexicon-powered warning card instructing users to visit on a PC to initialize their deployment.
- **Glass Panel Borders:** Completely eradicated the default subtle white borders around `.glass-panel` and `.glass-surface` global definitions to yield a much cleaner, seamless look for the Vault, Command Center, and all standard UniversalCards. Cleaned up explicit trailing border assignments in `DashboardStatTile` and `CommandScreenQuickLink` to fully match this new borderless aesthetic unless explicitly active.
- **ActionPill Migration:** Successfully deprecated and removed the legacy `ScreenUtilityBar` component from the entire OS architecture. Migrated all primary hub interfaces, ticket editors, diagnostics, and conflict managers to use the new `ActionPill` component, drastically improving mobile responsiveness by leveraging the new `leftContent` and `rightContent` flexible layout slots.
- **Side Panel Filter Standardization:** Replaced all remaining manual implementations of `FilterPopover` and `CustomDropdown` in Side Panel headers (`NexusSidePanels`, `NexusReportsViewer`, `TicketLogViewer`, `SanctuaryAlertsSidePanel`, `ArchitectHomesteadDiagnostics`) with the new, unified `ActionPill` pattern. This guarantees consistent search, filter tabs, and responsive UI behaviors globally.
- **Date Picker Glass Fix:** Refactored the `CustomDatePicker` popup to properly ingest `SidePanelContext` and apply the exact `backdrop-blur-md shadow-2xl portal-glass-fix` styling logic when rendering inside Side Panels, resolving an issue where the popup was far too transparent due to inherited panel overrides.
- **Dropdown Portal Positioning & Glass Transparency:** Fixed the "scattered panel issue" affecting dropdowns rendered inside SidePanels (`CustomDropdown`, `ModSearchDropdown`). Previously, the `portal-glass-fix` pseudo-elements were conditionally skipped when inside a SidePanel, resulting in fully transparent, illegible dropdowns. The style injection is now strictly enforced globally. Additionally, upgraded `CustomTierDropdown` to use dynamic `getBoundingClientRect()` positioning with native `scroll` and `resize` event listeners, matching standard shared UI dropdowns, ensuring it stays firmly anchored to its trigger button when the user scrolls the panel.
- **Side Panel Action Header Consolidation:** Migrated the bulky `footer` actions in `ArchitectConflictMatrix` and `MasonConflictsManager` into the unified `SidePanel` `headerActions` slot using sleek `PanelHeaderButton`s. Repositioned the confirmation dialogs (`deleteReason`, `updateReason`) inline directly into the form layout, streamlining the visual hierarchy and freeing up significant vertical space.
- **Side Panel Header Finalization:** Completed the final global phase of the Side Panel footer-to-header migration, moving action buttons into `headerActions` across all remaining hub editors, including `KeeperSupportSettings`, `SASupportSettings`, `IdentityMatrix`, and the `PushTemplateSidePanel`. Eliminated remaining legacy footers and relocated non-action forms seamlessly into the panel body, ensuring pristine visual consistency.

### Visual Overhauls
- **ActionPill UI Standardization:** Migrated 47+ hub and side panel components to use the new \ActionPill\ component and \CustomDropdown\ (with \flat={true}\ and \variant="pill"\) replacing the legacy \SearchBar\ and \FilterPopover\. This ensures a sleek, unified, stacked filter layout across all complex tables and lists throughout the Sanctuary OS interface.
- V8 Redesign for Conflict Radar Layout: 
  - Eradicated the hidden HoverTabDrawer and replaced it with visible inline PillTabs for instant navigation.
  - Corrected CommandScreenStats scope on the landing page, allowing the main statistics grid to render cleanly across the entire window rather than stacking inside the main body.
  - Implemented the 'Mason IDE' pattern on the Conflicts view by swapping the clunky GlassSegmentedControl with responsive DashboardStatTiles for seamless severity filtering.
  - Removed stark red/yellow horizontal borders for a sleeker presentation.
  - Corrected structural layout tags for the Overrides view to ensure it renders inside the CommandScreenLayout properly with an actions sidebar.
- V9 Visual Fixes for Radar: 
  - Restored the HoverTabDrawer navigation to fix missing side tab bar.
  - Removed the empty CommandScreenSidebars on the Conflicts and Overrides tabs which were crushing the main content column into a tiny sliver.
  - Integrated the 'Undo Overrides' button into the global top ViewHeader so it's always accessible without taking up vertical sidebar space.
  - Unified the view structure by applying CommandScreenSectionHeading to Conflicts and Overrides tabs, creating a clean visual break between the stat tiles and the data.
  - Purged the duplicate inline search bar on the Conflicts tab.
- V9.1 Layout Polish: 
  - Shrunk the search bars on the Conflicts and Overrides tabs so they no longer consume the entire screen width.
  - Eliminated the awkward inline Scope dropdowns, Overrides 'Active/Ignored' pill tabs, and floating Undo button from the top headers.
  - Consolidated all of these secondary actions/filters into a clean 'Radar Settings' SidePanel triggered by a single settings button within the search bar.
  - Removed the oversized subheaders from the Conflicts and Overrides tabs to return them to standard compact list views.
- V9.2 Settings Panel Redesign: 
  - Restyled the 'Scan Scope' selection options into a grid of interactive cards with folder icons, rather than full-width list items.
  - Replaced the basic Overrides 'Active/Ignored' list buttons with a unified GlassSegmentedControl for a much cleaner and standard aesthetic.
  - Registered all missing translation keys (radar_settings, scan_scope, override_filters, undo_overrides) in the lexicon JSON files to fix the raw bracketed text bug.
- V9.3 Radar Settings Cleanup: 
  - Purged the active-state 'dot' from the Scan Scope cards.
  - Removed the 'Override Filters' and 'Undo Overrides' buttons entirely from the Radar Settings side panel, as they were contextually misplaced.
- V9.4 Overrides Unification: 
  - Entirely removed the concept of filtering the Overrides tab by Active/Ignored.
  - The Overrides tab now renders a single unified grid containing both Active Overrides and Ignored Conflicts simultaneously.
  - Replaced the blue selection dot on Scan Scope cards with a clear radio button icon to improve visual feedback for the selected state.
- V9.5 Radar Overrides Stat Filters: 
  - Entirely removed the dot and radio buttons from the Scan Scope cards, relying purely on the glass tint and border to indicate active state as requested.
  - Implemented the DashboardStatTile filtering pattern for the Overrides tab. Active Overrides and Ignored Conflicts can now be toggled directly from the top stats bar, exactly like the Conflicts view, completely eliminating the need for PillTabs or SidePanels.
- V9.6 Radar UX Polish:
  - Re-applied localization keys to fix missing strings in the Overrides panel.
  - Scan Scope cards in the side panel are now correctly tinted with the accent color.
  - Running a Sweep on the Overview page now automatically navigates the user to the Conflicts page upon completion.
- **ModDossier Header Alignment:** Fixed an issue where Side Panels using `hideHeader={true}` alongside `forceShowCloseBtn={true}` (specifically `ModDossier` and its nested mini `kidToRender` child dossier) were rendering raw close buttons without the standard glass pill background container. Updated the rendering logic in `shared.tsx` to wrap these forced header buttons in the identical `glass-panel` pill styling used by standard panels, and ensured `headerActions` (like the equip/unequip button on the mini dossier) are correctly rendered alongside the close button even when the main header is hidden. Additionally, removed a redundant custom close button from the flag modal that was causing overlapping UI elements.
- **Blueprint Inspector Mobile Polish:** Refactored the statistics tiles (Game Version, Downloads, Published) at the top of the Blueprint Inspector (`NexusSidePanels.tsx`) to use a responsive grid layout. The tiles now smartly collapse on mobile devices—with the Game Version spanning the full width and the other two sharing a row—completely resolving the issue where they awkwardly stacked and consumed the entire vertical viewport.
- **Blueprint Inspector Filters Update:** Replaced the embedded `PillTabs` ("Global" vs "Missing Only") inside the artifact search bar with a standalone sleek `GlassSegmentedControl`. Updated the surrounding container to correctly stack on mobile viewports (`flex-col`), resolving an issue where the filters were previously hidden entirely on small screens.
- **Blueprint Inspector Lexicon Standardization:** Replaced hardcoded fallback UI strings ("Game Version", "Downloads", "Published", "Citizen", "UNKNOWN", "Premium") in the Blueprint Inspector top section with proper lexicon keys (`t("blueprint_version_label")`, `t("downloads_count")`, etc.). Two missing keys (`status_unknown`, `badge_premium`) were also globally generated and appended into the `en-default`, `en-sanctuary`, and `Simlish` dictionaries to guarantee proper thematic routing.
- **Desktop Dashboard Navigation Cleanup:** Successfully rolled out the new StatTileCarousel horizontal scrolling component across all remaining main application screens (Vault, Nexus, Blueprints, GlobalFeed, Conflict Radar, and TimeCapsule). Redundant, bulky DashboardStatTiles that previously clustered on the Overview screens have been systematically removed and their key metrics transferred. Missing metrics (such as Sandbox and Archives counts in Vault, active Blueprint names, and total Nexus counts) were correctly injected directly into the tab tiles, while static readouts and action-oriented items (like Total Storage or My Replies) have been elegantly reorganized into either the StatTileCarousel or the right-side CommandScreenQuickLink sidebars for a much cleaner unified desktop experience.
- **Lab Builder Responsiveness:** Fixed an issue in `Lab.tsx` where the left and right columns on the Builder tab did not stretch dynamically to match each other's heights or fit the screen appropriately. Replaced static `h-max` container sizing with flexible `flex-1` assignments, removed hardcoded padding, and converted the right column modifiers into a responsive grid (`lg:grid-cols-2`) so the "Symbiotic" and "Adversarial" boxes stack neatly side-by-side on wider viewports instead of stretching to massive widths.

### Bug Fixes
- **Uplink Codes Count Initialization:** Fixed an issue in `Blueprints.tsx` where the network Uplink Codes count would not populate on initial page load, remaining at 0 until the user explicitly navigated to the Network tab. The fetch logic is now initialized correctly on component mount regardless of the active tab.
- **Lab Builder Responsiveness:** Fixed an issue in "Lab.tsx" where the left and right columns on the Builder tab would overflow and 'hang off the side of the window' on smaller screens. Removed shrink-0 from the CommandScreenSectionHeading to allow long text strings like 'Symbiotic Dependencies' to properly truncate instead of forcing the UI off-screen. Applied missing pr-4 padding to the main Lab container to ensure content never touches the scrollbar on the right edge. Fixed text truncation bugs in the Staged and Conflict extra pills by ensuring they use lex-1 min-w-0.
- **Lab Builder Grid Blowout Fix:** Corrected a CSS Grid layout bug in Lab.tsx where the right column's uto minimum width was forcing the grid to expand beyond the viewport on mid-sized screens. Updated the grid-cols layout to use minmax(0, 1fr) and minmax(0, 2fr) explicitly, ensuring the columns are permitted to shrink down dynamically. Also applied min-w-0 flex properties to the heading components to resolve residual width stretching.

- **Lexicon Patch:** Restored missing dictionary keys (status_tag_stable, status_tag_under_review, status_tag_pending, status_tag_unknown, status_tag_unstable) across all three localization dictionaries.
- **Mobile Navigation Stability:** Fixed the HoverTabDrawer crash by injecting mobileOpen states, restoring global mobile navigation feeds.
- **Stat Tile Unification:** Standardized the DashboardStatTile component so that ariant=tab tiles have an explicit, uniform width (w-[160px] md:w-[260px]), fixing horizontal overflow clipping where longer labels would push sibling tabs off-screen on mobile devices.
* Fixed mobile navigation across Nexus and GlobalFeed by enabling the horizontal StatTileCarousel and removing the drawer-based mobile layout.
* Resolved StatTile overflow by implementing dynamic w-[calc(50vw-1.375rem)] sizing to allow a clean two-tile-wide display on mobile devices.
* Restored dynamic counts to Mason Registry StatTiles.
* Fixed HoverTabDrawer text visibility on mobile by using --text instead of --sidebartext.
* Updated Mason Registry layout logic: Overview now renders compact horizontal list-style cards, while deep-dive tabs render vertical cards in a responsive grid.
* Resolved duplicate navigation entries in the Desktop Sidebar.
* Fixed mobile ActionPill width constraint to fully extend horizontally.

### Visual Overhauls
- **UI Refinements:** Fixed mobile menu clipping by adjusting scroll padding, removed duplicated Mason Hub navigation blocks in the sidebar, applied a max-width constraint to desktop search bars in the Registry, and converted the Registry Overview large vertical cards into a sleek horizontal layout.
- **Registry Overview Styling:** Replaced the horizontal list layout in the Registry Overview with a responsive grid of smaller, more compact artifact cards.
- **Mobile Navigation Fix:** Fixed a bug in the Mobile Bottom Nav where the Mason Workshop active icon would not properly highlight or display its text label when inside the hub.
- **Layout Fixes:** Corrected StatTile margins so they perfectly align with content on desktop, stretch across the entire screen on mobile, and reduced the vertical padding so they fit more comfortably.

- **Mason Layout Overhaul:** Re-aligned the global ViewHeader to prioritize a fluid lex-row alignment on Desktop viewports, preventing the global Search Bar from breaking onto a new line and pushing down nested content. This completely resolves the massive vertical drift seen in the Mason Workshop when compared to other core hubs like the Vault. Furthermore, rebuilt the mobile registry StatTileCarousel with standard -mx-6 wrappers to seamlessly extend edge-to-edge on mobile, perfectly matching the visual parity of the VaultTabs drawer navigation.
